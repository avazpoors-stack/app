# MEMORY.md — حافظهٔ دائمی پروژه (اپ فولاستک اندروید)

> **چرا این فایل؟** حافظهٔ چت/مدل بین جلسه‌ها باقی نمی‌ماند. این فایل «حافظهٔ بلندمدت» پروژه است و در گیت‌هاب ذخیره می‌شود.
> **روش کار در هر جلسه:** ۱) اول همین فایل را کامل بخوان. ۲) حرف‌ها، تحقیقات و تصمیم‌های جدید کاربر را با تاریخ به بخش‌های مناسب اضافه کن. ۳) در پایان جلسه commit و push کن.

## پروژه در یک نگاه
- هدف: اپ فول‌استک اندروید «فیتنس لایف‌استایل» + بک‌اند + دیتابیس — (user, 2026-08-09)
- ریپو: github.com/avazpoors-stack/app — برنچ کاری: arena/019fe750-app
- وضعیت: برنامه‌ریزی (پروتکل Dry-Run Interview در جریان است)

## قواعد شواهد (برای همهٔ جلسات)
هر ادعای واقعی باید یکی از این برچسب‌ها را داشته باشد:
- `(user)` — خود کاربر گفته
- `(verified: <منبع>)` — این جلسه از منبع مشخص راستی‌آزمایی شده
- `[assumed: پیش‌فرض — اگر اشتباه: ...]` — حدس آشکارشده با ریسک

## ۱) درخواست کاربر (harvest)
- «می‌خوایم یک app full-stack واسه اندروید بسازیم» — (user)
- «همهٔ تحقیقات و نکاتی که بهت می‌گم رو جوری ذخیره کن» — (user)
- «هوشمند با حافظهٔ کامل باش واسه نوشتن برنامه؛ بقیه رو توی حافظه‌ات/گیت‌هاب می‌ذارم» — (user)

## ۲) یافته‌های راستی‌آزمایی‌شده (recon, 2026-08-09)
- ریپوی `app` فقط README دارد («fitnes life stile»)؛ هیچ کدی نیست — (verified: ls، git log، git ls-remote)
- ابزارهای نصب‌شده در محیط: node v22.22.3، npm 10.9.8، python 3.11.2، gh 2.23.0 — (verified: دستورات --version)
- جاوا / Android SDK / فلاتر / داکر در محیط نیست؛ ساخت APK نیاز به نصب toolchain یا استفاده از CI دارد — (verified: command -v)
- ریپوی پژوهشی `avazpoors-stack/posture-ai-research` («بادی پاسچر»):
  - ابزار پژوهشی غربالگری پاسچر با عکس استاندارد (front/back/side)؛ کاربر هدف: پژوهشگر/فیزیوتراپیست؛ صراحتاً «ابزار تشخیص پزشکی نیست» — (verified: README.md آن ریپو)
  - وضعیت: P0 کامل، P1 در جریان (Pilot با ۱۲–۲۰ فرد رضایت‌داده)، P2 به بعد مسدود — (verified: docs/ROADMAP_FA.md)
  - پایتون ≥3.10؛ وابستگی‌ها: PyYAML، Pillow؛ dev: pytest؛ desktop: PyInstaller — (verified: pyproject.toml)
  - حریم خصوصی: عکس/CSV/database/log/model نباید وارد Git شوند — (verified: README.md آن ریپو)

## ۳) تصمیم‌های گرفته‌شده
| تصمیم | مقدار | وضعیت |
|---|---|---|
| سازوکار حافظهٔ دائمی | MEMORY.md + commit/push به گیت‌هاب | گرفته شد 2026-08-09 |
| زبان اسناد/یادداشت‌ها | فارسی (کد و دستورها انگلیسی) | [assumed: کاربر فارسی می‌نویسد — اگر اشتباه: انگلیسی] |

## ۴) سؤالات باز مصاحبه (Interview)
- Q1 (2026-08-09): اکشن اصلی اپ و ارتباطش با پروژهٔ پاسچر → جواب: ⬜
- Q2: منطقه/مخاطب (ایران/خارج؛ استور/APK؛ پرداخت) → جواب: ⬜
- Q3: استک موبایل (نیتیو Kotlin+Compose یا Flutter) و بک‌اند → جواب: ⬜

## ۵) تحقیق و نکات کاربر (Research & Tips)
<!-- هر نکته/تحقیقی که کاربر می‌دهد، اینجا با تاریخ و برچسب شواهد اضافه شود -->
(هنوز چیزی ثبت نشده)

## ۶) نکات فنی و محدودیت‌های محیط
- ساخت APK در این محیط نیاز به نصب JDK + Android SDK دارد (حدود ۱GB دانلود) یا می‌شود از GitHub Actions با image استاندارد اندروید استفاده کرد — (verified: toolchain check)
- بک‌اند: پیش‌فرض Python + FastAPI به دلیل اکوسیستم پایتونی موجود کاربر [assumed] — در Build Phase 1 راستی‌آزمایی می‌شود
