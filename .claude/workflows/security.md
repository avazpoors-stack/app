# Security Workflow
project-memory → project-analyzer → authentication-security → privacy-auditor → security-tester → medical-safety-auditor when relevant → code-reviewer → final-qa-gate

Do not expose secrets in reports or logs.