# Bugfix Workflow
project-memory → project-analyzer → flutter-tester/reproduction → debugger → minimal implementation → targeted tests → code-reviewer → final-qa-gate

Do not refactor unrelated code. Reproduce or establish evidence before claiming fixed.