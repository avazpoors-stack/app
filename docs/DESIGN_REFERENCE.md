# مرجع طراحی (Design Reference) — اپ «بدنه»

> وضعیت: جمع‌آوری شده 2026-08-09 · برچسب شواهد: رنگ‌ها و سلیقه از خود کاربر (user)؛ فایل‌های تصویری از جستجوی وب (verified: image_search).

## تصمیم کاربر / حامی
- حامی مالی از تم نرم‌افزار **بام بانک ملی (baam.bmi.ir)** خوشش می‌آید و می‌خواهد تم اپ شبیه آن باشد — (user, 2026-08-09)
- مشخصهٔ ظاهری که کاربر نام برد: **نارنجی-خاکستری (orange-gray)** — (user)
- توجه: کاربر ابتدا لینک ebank.bmi.ir داد و سپس تصحیح کرد: «منظورم baam.bmi.ir است، رنگ نارنجی خاکستریه» — (user)

## فایل‌های مرجع (ذخیره‌شده در همین پوشه)
| فایل | منبع |
|---|---|
| baam-bmi-ir-1.jpg تا 4 | جستجوی تصویر وب برای «baam.bmi.ir سامانه بام» — (verified: image_search 2026-08-09) |
| ebank-bmi-ir-1.png تا 4 | جستجوی تصویر وب برای «سامانه بام بانک ملی ebank.bmi.ir» — (verified: image_search 2026-08-09) |

## نکات راستی‌آزمایی
- صفحهٔ زندهٔ baam.bmi.ir در این محیط قابل دریافت نبود (سایت بانکی، fetch مسدود) — (verified: fetch_page failed)
- این محیط قابلیت دیدن تصویر ندارد؛ رنگ‌ها و ساختار دقیق UI باید در Phase طراحی از روی فایل‌های بالا توسط انسان یا ابزار دارای vision استخراج شود — [assumed: فایل‌های تصویری نمایندهٔ ظاهر بام هستند — اگر اشتباه: از حامی اسکرین‌شات دقیق بگیرید]

## سازگاری با پالت مسترپلن «بدنه» (verified: docs/BADANE_MASTERPLAN.md بخش ۱.۱)
- مسترپلن: مشکی زغالی #121212 پس‌زمینه + نارنجی #FF6B35 برای اقدام + طلایی #FFB800 و سبز #4CD964 برای موفقیت
- جهت تم بام (نارنجی-خاکستری): نارنجی #FF6B35 به‌عنوان رنگ اکشن حفظ می‌شود؛ پس‌زمینه‌ها از مشکی خالص به طیف خاکستری نزدیک می‌شوند [assumed: تفسیر «نارنجی-خاکستری» — اگر اشتباه: اسکرین‌شات دقیق از حامی بگیرید]
- قانون: تغییر ظاهر، تغییر هویت محتوا/لحن نیست؛ همهٔ بخش‌های دیگر مسترپلن بدون تغییر می‌مانند

## پیش‌نمایش مفهومی UI (تولیدشده)
- `docs/design-reference/ui-preview-home.png` — پیش‌نمایش مفهومی صفحهٔ خانه با تم نارنجی-خاکستری بام (تولیدشده با AI در 2026-08-09)
- ⚠️ این تصویر فقط جهت انتقال حس بصری است؛ متن فارسی تولیدشده توسط مدل تصویر ممکن است غلط املایی داشته باشد. UI نهایی در Flutter با فونت فارسی استاندارد ساخته می‌شود.
